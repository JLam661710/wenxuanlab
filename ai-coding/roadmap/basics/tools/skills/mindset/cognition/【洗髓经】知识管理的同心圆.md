## 🗺️ 认知减负：为知识划定“停火线”

Vibe Coding 时代的知识折叠术

对于新手来说，Vibe Coding 最大的敌人不是代码，而是“信息过载” (Information Overload)。
当你打开一个项目，看到 `package.json`, `tsconfig.json`, `webpack.config.js`，以及无数个新名词（SSR, Hydration, JWT, CORS）时，那种窒息感足以劝退 90% 的人。

“自由探索”在此时不是蜜糖，而是砒霜。我们需要为学习者划定明确的知识边界。

### 1. 知识的“同心圆理论”
我们把编程知识分为三个圈层，对不同圈层的知识采取不同的态度：

*   核心圈 (Must Know) —— 必须死磕
    *   *定义*：不掌握就无法与 AI 对话，无法理解 Vibe Coding 的本质。
    *   *内容*：
        *   基础语法：变量、循环、函数（能看懂逻辑即可）。
        *   数据结构：JSON 对象、数组（知道数据长什么样）。
        *   网络协议：HTTP 请求（GET/POST）、API Key（知道数据怎么传输）。
        *   环境常识：终端基本指令 (`cd`, `ls`)、端口号 (`localhost:3000`)。

*   辅助圈 (Should Know) —— 知道名字即可
    *   *定义*：AI 会帮你写，你只需要知道“这是干嘛的”，以便在报错时能定位。
    *   *内容*：
        *   框架概念：React 组件、Hooks、Vue 指令。
        *   数据库：SQL 语句、ORM。
        *   样式：Tailwind CSS 类名。
    *   *策略*：“不求甚解”。看到 `useEffect` 知道它是“副作用”就行，不用去背诵它的依赖数组规则。

*   外围圈 (Nice to Know) —— 完全黑盒化
    *   *定义*：底层原理和复杂配置，完全交给 AI 或框架处理。
    *   *内容*：
        *   构建工具：Webpack/Vite 配置、Babel 转译原理。
        *   底层机制：V8 引擎垃圾回收、React Fiber 架构、TCP 三次握手。
    *   *策略*：“无可奉告”。除非你是为了面试，否则在 Vibe Coding 阶段，请把它们当成魔法。

### 2. 也是一种“工程化思维”
把知识分为“应用级”和“科研级”，本身就是一种工程智慧。
*   比喻：
    *   Vibe Coder 是赛车手：你需要知道如何换挡、如何过弯（核心圈），需要知道发动机大概的工作原理（辅助圈），但你不需要知道汽油在气缸里燃烧的热力学方程（外围圈）。
    *   传统程序员是机械师：他们需要懂每一个螺丝钉。

> 给学员的定心丸：
> 即使是阿里 P8 架构师，也不可能精通所有技术栈。
> Vibe Coding 的核心能力，是“调用知识”的能力，而不是“背诵知识”的能力。
> 当你面对一个陌生名词时，问自己：“如果我不懂它，我的代码还能跑吗？”
> 如果能，那就大胆地忽略它。
