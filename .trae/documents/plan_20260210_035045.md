我们将把代码库回退到文件重命名和 Rollup 配置修改之前的状态，并保留 Cloudflare 部署配置。

### 📅 执行计划

1.  代码回退 (Hard Reset)
    *   执行 `git reset --hard cb82eba`
    *   这将撤销最近的“文件重命名”和“Rollup 配置修改”，将项目完全恢复到中文文件名和目录结构的状态。
    *   预期结果：所有文件变回中文名，`.vitepress/config.mts` 恢复原样，网站内容和导航栏恢复正常。

2.  恢复 Cloudflare 配置
    *   回退操作会删除刚刚创建的 `wrangler.jsonc`。
    *   我将重新创建这个文件，确保我们仍然可以尝试使用 Cloudflare 进行部署。

3.  强制推送到 GitHub
    *   执行 `git push -f origin main`
    *   这将覆盖 GitHub 上的记录，使远程仓库也回到“正常的旧版本”。

4.  验证与下一步
    *   回退完成后，您的本地网站将立即恢复正常。
    *   Cloudflare 会自动检测到推送并尝试重新部署。
    *   如果 Cloudflare 对中文长文件名支持较好，部署可能会直接成功；如果仍然报错，我们再针对性地解决（例如只修改极个别超长文件名，而不是全量修改）。

### ⚠️ 注意事项
*   这将丢失所有“重命名”和“配置哈希文件名”的修改记录（这正是您想要的）。
*   请确认您不需要保留这些修改的任何备份。
